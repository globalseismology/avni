name: Build docs on PR
on: push
jobs:
  build-and-preview:
#    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - name: Prerequisites
        run: conda install sphinx=4.3.0
          && conda install sphinx_rtd_theme=1.0.0
          && conda install sphinxcontrib=1.0
          && conda install sphinxcontrib-bibtex=2.4.1
      - name: Checkout repo
        uses: actions/checkout@v2
      - name: Build docs
        run: cd docs
          && sphinx-apidoc -o api/ ../avni
          && make html

#        run: cd docs_sphinx
#          && make html
#          && cd _build/html
#          && touch .nojekyll
#          && mkdir ../../../public
#          && cp -r * ../../../public
#      - name: Deploy
#        uses: FirebaseExtended/action-hosting-deploy@v0
#        with:
#          repoToken: '${{ secrets.GITHUB_TOKEN }}'
#          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_DOXYGEN_TEST }}'
#          projectId: doxygen-test
#          expires: 7d
