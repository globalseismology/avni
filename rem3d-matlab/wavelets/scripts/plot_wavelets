%Script to plot a given wavelet or wavelets on the sphere. 

%Essentially, can hard build a synthetic model element-by-element
%by scratch with this. Just be aware of where the scales are meant to be on
%the cubed sphere. 

%Anant Hariharan

Desired_Index_dim1 = [1];
Desired_Index_dim2 = [1];
face= [1];
Desired_Coefficient =[10];

N = 7;
Jmax = 4;

[vwlev,vwlevs] = cube2scale(N,[Jmax Jmax],1);

for i = 1:length(Desired_Index_dim1)
    vwlevs(Desired_Index_dim1(i),Desired_Index_dim2(i),face) = Desired_Coefficient(i)
end

reconstructed_synthDv = angularD4WT(vwlevs,[Jmax Jmax],[1 1],'inverse',1);

figure

h=imagefnan([1 1],[2^N 2^N],reconstructed_synthDv);
